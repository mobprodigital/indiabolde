import { ImageSliderDirective } from './image-slider.directive';

describe('ImageSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageSliderDirective();
    expect(directive).toBeTruthy();
  });
});
